services:
  - type: web
    name: exu-coder-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bot:app & python bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
